// client/src/pages/buyer/components/Footer.jsx
// Kept as a thin re-export so existing buyer imports stay valid; the markup
// now lives in the shared HubFooter used by every dashboard.
export { default } from '../../../components/hub/HubFooter';
